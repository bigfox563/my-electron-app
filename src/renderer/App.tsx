import React  from 'react'

export default function App() {

  const handleClick = () => {
    window.electron;
  };

  return (
    <div>
      <button onClick={handleClick}>Click me</button>
    </div>
  )
}